# AI Tutor Frontend (Next.js)

## Run locally
```bash
cd frontend
npm install
cp .env.local.example .env.local
npm run dev
```

Make sure backend is running on `http://localhost:8000`.

## Pages
- `/` Chat + practice panel
- `/skills` Skill dashboard

- `/profile` User profile + progress goals
